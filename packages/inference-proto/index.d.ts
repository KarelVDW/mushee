/** Absolute path to the inference .proto, for @grpc/proto-loader consumers. */
export const PROTO_PATH: string;
/** Proto package name (`mushee.inference.v1`). */
export const PROTO_PACKAGE: string;
